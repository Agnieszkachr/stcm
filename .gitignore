# STCM
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/
*.pkl
*.pt
*.bin

# Data (large files — download fresh)
data/raw/*.txt

# Cached embeddings
data/processed/emb_*.pkl

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db
desktop.ini

# Env
.env
.venv/
venv/

# Jupyter
.ipynb_checkpoints/

# NOTE: in this repository `site/` is the MkDocs *source* (docs_dir) and
# `docs/` is the built site published via GitHub Pages — both are tracked.

# PDFs / Books
*.pdf

# Article drafts and manuscripts (never publish with the repo)
article*
anonymous*
cover_letter*
nts*
*.docx
*.doc

# Local scratch
*.log
*.tmp
*.bak
